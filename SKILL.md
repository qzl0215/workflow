---
name: workflow
description: 通用状态变更工作流。表面请求与真实用户结果需推断时澄清，不要求用户先说“澄清需求”；默认 H0/H1，复杂才升级 H2/H3。不用于纯问答和一次性只读操作。
version: 2.22.0
author: zhonglin
license: MIT
---
# workflow

workflow 是 dependency-closed 的通用状态变更工作流，只安装本目录即可。

## 使用者先看这一条链

- 计划：需求澄清 → 选定方案 → 拆成任务
- 执行：执行任务 → 验收交付
- 复盘：提炼经验 → 回灌改进

完整主链：需求澄清 → 选定方案 → 拆成任务 → 执行任务 → 验收交付 → 提炼经验 → 回灌改进。

## 不变量

1. 一切围绕 AI 高 ROI：优先增加用户结果、降低关键风险和减少未来重复成本；低价值环节可省，高价值判断不可因 AI 执行快而省。
2. 调研按项目真源、已连接工具与数据、官方一手来源、最小实验推进；当结论依赖可变项目现状时，新需求进入调研前先用项目定义的 freshness 机制刷新目标真源并固定 source fingerprint，新增证据不再改变目标、范围、方案或风险时停止。
3. 表面请求与真实用户结果之间仍需方向性推断时，AI 主动进入需求澄清，不依赖用户说出“澄清需求”；标准/项目任务在调查后给“推荐需求契约（待确认）”，用户明确确认前不通过。
4. 一次只持久化一个当前动作；出口不满足就回到产生缺口的动作，不把纠错、体验或会诊另记为阶段。
5. 证据驱动检查点可以快速通过，但硬门不得跳过；执行速度不能替代业务价值、验收证据和授权。
6. 能补现有真源就不建文档；长期知识只答“何时用、改变什么、怎样找到唯一真源”，答不清就不沉淀，临时证据及时删除。同一决策主题只保留一条当前有效的 decision receipt；最新目标覆盖旧结论，有风险才确认，不留翻转流水。
7. 未经明确授权，不触发外部副作用；交付只用原生入口或 `safe_merge.py`，不调用外部提交 skill。
8. 没有 fresh 证据，不声称完成；回灌提案不等于写入授权，确认后实施。

## 入口分流（不计入阶段）
**默认快刀**：先采用最低充分流程，只有真实复杂度信号才升级，不把机械规模当风险。
- **轻任务**：答案明确、影响局部、一次安全操作可闭环。直接处理、做相称验证并快速通过；不建立项目文档，不降低验收。
- **标准任务**：需要多个相关动作，但方向明确、风险可控且可在一个任务中闭环。只走必要动作，计划和复盘默认留在对话中。
- **项目任务**：只有出现难逆产品或架构决策、数据迁移、权限、安全或生产风险、跨系统兼容性、需求方向不明确、连续失败或证据冲突、确需跨会话/并行协作/长期恢复时升级，建立最小文件真源。

文件数量、项目重要、常规发布或修改 workflow 自身不能单独触发升级；交付副作用可按需加载授权交付 harness，而不升级整项工作。七动作是证据驱动检查点，不是七场固定仪式；已有输入、判断和证据可快速通过，但不降低验收、不扩大授权或伪造工作。

**通用能力唯一入口**：workflow 激活后，通用规划、执行、调试、验证、Git 收尾和复盘不叠加同类 standalone skill；仅用户明确点名的兼容入口或结果独立的专业能力可叠加。当前动作外的 reference 不得预读；UI harness 仅在需求确认后、仍会改变方案时加载，有批注定向稿则快通。

## 状态接口与硬门

| 阶段 | 已知输入 | 主要产物 | 通过证据 | 缺口返回 |
|---|---|---|---|---|
| 需求澄清 | 请求、事实、约束、已有真源 | 目标、价值、验收、未知分流与授权边界 | 价值门 A、调研充分性硬门与需求成熟度硬门通过，关键用户判断已有回答；标准/项目任务的需求契约已获明确确认 | 继续调查、关键追问或 blocker |
| 选定方案 | 已确认目标、证据、约束 | 90 分最终画面、推荐方案、取舍、边界与回滚点 | 价值门 B 通过，方向性未知已收敛或转为可验证假设 | 需求澄清 |
| 拆成任务 | 已选方案；UI 范围另有体验结论 | Plan/Task、依赖、文件域、owner、验收和验证路径 | 用户确认完整 Plan，每个 Ready Task 可独立执行 | 需求澄清或选定方案 |
| 执行任务 | 确认计划、当前 Ready Task | 范围内产物、diff、进度和 Task 级证据 | 产物满足 Task 验收且有 fresh evidence | 上游缺口、纠错回路或 blocker |
| 验收交付 | 实际产物、验收；按需加载项目发布真源与授权 | Task→Plan→业务证据；有交付目标时补真实交付状态 | 证据门通过；条件交付的授权与发布门按实际目标通过 | 执行任务、上游缺口或 blocker |
| 提炼经验 | 计划、结果与偏差证据 | 最小知识候选、no-op 或例外证据去向 | 候选说明使用场景、核心决策价值和唯一真源入口 | 对应产生偏差的动作 |
| 回灌改进 | 痛点证据、回灌提案、用户决策 | 目标 owner 的语义整合或 no-op | 消费价值与可发现性门、用户确认和 fresh 验证通过，外部副作用已授权 | 提炼经验、拆成任务或 side task |

只持久化一个 `stage`，新写入只允许七个中文值。Plan/Task 的执行状态由独立的 `status` 字段持有，不得混入 `stage`。旧值只在读取边界归一，正式入口、reference 和新文档不再写旧词；无法归一的值 fail closed。

## 用户交接

**决策价值过滤器**适用于所有用户可见内容：优先表达用户结果、实际效果、真实代价、关键风险和必要行动。细节仅在会改变决定、行动、风险认知或对结论的信心时进入主回复，否则删除或按需提供；无取舍时不制造决策，无证据时不虚构业务价值，不得隐藏失败事实、关键风险、授权边界和必要证据。除等待用户回复外，H0/H1 只在开始、真实 blocker、完成时简短播报；H2/H3、跨上下文时，状态变化后下一条消息给完整快照再继续工作。

进度｜■■■◆□□□ 4/7 · 执行任务
技能｜[执行任务](references/execute-tasks.md) · [修复失败](references/fix-failures.md)
成果｜✓ [需求澄清](resolved/findings.md) · ✓ [视觉方案](resolved/selected-preview.html) · ● 计划 P01–P05 · ○ 交付 · ○ 进化
✓ P01 → ● P02 / T03 → ○ P03

`技能`只列实际读取的底层 reference，用一级中文标题和 Markdown 链接。状态用普通文本和普通 Markdown；Plan 只显最短活动路径；不展示 Ready 队列、不探测宿主、不生成 DAG 视觉、不建立第二状态真源。

只有需要人参与时才交回控制权，如纠正、选择、确认、外部输入、授权、解锁或验收。**每次等用户回复前，必须在同一消息顶部展示更新后的完整快照，不受 H0–H3 或状态变化限制；不得只提问或省略进度。**快照列阶段、已读 reference、成果，以及 Plan 时的最短路径。可查事实由 AI 调查；其余连续推进。收尾复盘门：完成前判断是否复盘。

交接顺序：**结论 → 风险/待决策（按需）→ 建议下一步 → 回复建议**。

`建议下一步｜<推荐动作>`
`回复建议｜<最短回复>`

快照不得替代 `下一 Ready`；非交接不生成。**成功交付例外**：没有风险、待决策或必要行动时，只回“已提交 GitHub，已合并，已发布：[链接](URL)”，不再重复完整快照。

## 阶段成果路由

项目任务在 `task_plan.md` 维护唯一活动成果路由：已通过且当前有效的每个完成阶段最多一个入口；单一业务成果直达文件，选定方案优先指向已选视觉预览，没有视觉方案指向 PRD/方案文档，多个同等重要成果指向已有目录。

真源只保存项目相对路径和 `document / visual / collection`，不得保存机器绝对路径，且不得保存 `file://` 或宿主状态；展示时解析为普通 Markdown 链接，不探测宿主。当前和待开始阶段不链接；上游变化移除受影响的下游活动入口，历史证据留在 findings/progress。

## 需求澄清中的四路未知

四路未知不是阶段，而是决定下一步处理方式的分流；处理完仍回到当前动作或主链。

| 未知类型 | 本质 | AI 处理 | 返回条件 |
|---|---|---|---|
| 事实可查 | 缺少客观事实 | 查项目、数据、工具、配置和可信来源，不把可发现事实问给用户 | 事实有来源，或证实不可得 |
| 取舍待定 | 缺少决策 owner 的选择 | 给推荐和代价；依赖、难逆或压力测试时进入 Grill 深度 | 决策 owner 作出选择 |
| 假设待验 | 讨论无法证明真假 | 定义最小实验、原型、小样或可失败验收 | 假设被证实、证伪或明确保留风险 |
| 外部待解 | 依赖范围外事实或权力 | 写清 blocker、外部 owner、解锁条件和安全等待方式 | 获得授权/事实，或调整范围 |

## harness 深度

AI 按动作自动选择最低但足够的深度；用户不必先学会挑工具。

| 深度 | 适用信号 | 行为 |
|---|---|---|
| H0 | 结论已证实、影响局部、可逆 | 复用证据并快速通过 |
| H1 | 常规任务、单一路线、风险可控 | 默认单视角完成必要检查，不加载专家、subagent、独立 Reviewer 或方法包 |
| H2 | 多方取舍、跨域、重要体验或并行价值明确 | 加载一个或多个按需 harness，并说明为什么需要加深 |
| H3 | 高风险、难逆、重大未知或生产级真实影响 | 独立视角交叉质询、原型/实验与加强验证；H2/H3 都必须说明为什么需要加深 |

H0/H1 是默认深度；H2/H3 只能由表中真实信号升级，文件多、项目重要、发布或自修改不是加深理由。价值门、Plan 确认、证据验收门和授权交付硬门不得降级；可用能力下降时只改变手段，不降低判断深度，不把未覆盖写成通过。

## 按需路由

默认只读当前 owner；harness 串行按需加载，未来 reference 不预读。每个 reference 由根入口路由一次，reference 之间不互相深链。owner 与 harness 不要求数量相等；能力只有同时拥有独立问题、不同退出条件、唯一触发、低共触发率和单独加载收益才独立成文件，平台机械件归 adapter。

| 类型 | 触发信号 | 仅读取 |
|---|---|---|
| 主 owner | 真实结果、事实或未知仍不清楚 | `references/understand-goal.md` |
| 主 owner | 需要勾勒最终画面、专家森林或方案取舍 | `references/decide-solution.md` |
| 主 owner | 方案已定，需要 Plan/Task、必要依赖图和验收 | `references/plan-tasks.md` |
| 主 owner | 已有确认计划，需要逐 Task 实施 | `references/execute-tasks.md` |
| 主 owner | 准备声明 Task、Plan 或业务结果完成 | `references/verify-results.md` |
| 主 owner | 交付后判断这次应沉淀什么 | `references/learn-review.md` |
| 主 owner | 候选经验需要进入长期真源 | `references/evolve-system.md` |
| harness | 需求已确认，且 UI 的内容设计、信息架构、视觉方向、交互、动效或无障碍仍会改变方案 | `references/shape-experience.md` |
| harness | 多个界面需要稳定、唯一设计真源 | `references/maintain-design.md` |
| harness | 需要独立视角、并行实施或 fresh review | `references/coordinate-agents.md` |
| harness | bug、测试失败、异常、空结果或连续失败 | `references/fix-failures.md` |
| harness | 上下文将满、跨会话或切换执行者 | `references/handoff-context.md` |
| harness | 已验收结果需要 commit、push、merge、deploy、公开发布或其他外部写入 | `references/deliver-release.md` |
| adapter | 同仓并行成果需要独立 worktree、串行 merge 与安全回收 | `adapters/merge-parallel-work.md` |
| 方法包 | 方案瓶颈需要大厂方法论时，默认只加载一个主方法包和一个挑战方法包 | `methods/strategic-value.md` · `methods/essence-subtraction.md` · `methods/experiment-attack.md` · `methods/delivery-compounding.md` |

## 文件真源与文档预算

- 简单任务默认零新增项目文档。
- 标准任务默认零新增项目文档；只有需要持久恢复时才创建一份最小 `task_plan.md`，计划能留在对话就不落盘。
- 复杂项目只有真实恢复需求时才启用热真源，默认放在 `<project>/plans/<yymmdd-summary>/`：
- `task_plan.md`：范围、Plan/Task、owner、验收、必要时的依赖图和唯一 `status` 真源。
- `findings.md`：事实、来源、推断、未知、比较和决策依据。
- `progress.md`：实际动作、失败、验证证据和 handoff。
- `implementation-plan.md`：仅在复杂实施、委派、跨会话或写冲突需要技术导航时创建；不保存状态。
- `index.md`：仅在多个活动任务需要统一定位时创建，不把完成 Plan 带入默认上下文；旧索引只读 `Active` 区段，到下一个同级标题即停，不得整文件读取。
项目知识复用 `AGENTS.md`、README、TRUTH、代码、配置和测试，入口只导航。长期知识须过消费门，临时证据及时删除，必要例外用现有系统；Plan 完成即退出热路径，退休检查后再删。
新文档答“何时用、改变什么、怎样找到唯一真源”，否则不建。知识语义整合进真源，仅真源或路径变化时更新导航；不新建归档文档或证据管理流程。

## 角色、权限与降级

- **总协调者**：判断当前动作、维护 Ready Queue、分配 owner，唯一更新范围和状态真源。
- **Plan owner**：对业务结果、集成和 Plan 级验收负责；不能用 Task 绿灯替代整体结果。
- **Task owner**：只在 capsule 的文件域和授权内实现，返回实际变更、证据、失败和上下文请求。
- **Reviewer**：从验收和 fresh diff 独立复核，不继承实现者的完成判断。

角色可以由同一 AI 顺序承担，但职责边界和授权不变。无 subagent 时 solo 执行同一契约；无 memory 时只用文件真源恢复；无 Git 时交付文件与证据；无执行工具或权限时停在可执行计划或 blocker。任何降级都不得把未验证写成已完成。

## 维护
修改本包必须走“回灌改进”：提案获确认后先写可失败验收，再改唯一 owner，并同步所有 writer、reader、测试和派生文档。生成内容只能从正式真源重建，不手改派生产物。

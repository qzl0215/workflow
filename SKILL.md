---
name: workflow
description: 通用状态变更工作流。以真实用户结果为主线，按风险渐进展开目标、计划、执行、验真、交付与复盘；不用于纯问答和一次性只读操作。
version: 3.7.0
author: zhonglin
license: MIT
---
# 工作流

本工作流用于把“想做一件事”推进到“真实结果已被证据证明”。它约束结果、边界和证据，不替模型规定固定题数、轮数、智能体数量或唯一做法。

## 一条结果主链

用户可见的核心主链只有：

**目标框定 → 结果规划 → 任务执行 → 结果验真**

完成验真后，再按真实需要进入：

- **真实交付**：结果必须提交、合并、部署、发布或写入外部系统时进入。
- **经验复盘**：每个面向用户的目标在完成陈述后先做一次最小优化识别；发现能改变未来行动的问题时进入，形成问题诊断与待确认方案，没有可行动发现就简洁说明 `no-op`。

真实交付存在时，复盘以实际交付状态为输入，不默认与交付并行；可以提前收集候选观察，但不能在交付结果未知时写成最终经验。结果稳定后先向用户陈述已经完成的事实，再进行优化识别；后续优化作为新的目标，经用户确认后执行，不改变原任务已经成立的完成结论。

阶段是用户理解进展的投影，不是要求模型举行固定仪式。状态变更任务在执行前有两个用户可见的硬门：先完成需求澄清并取得需求确认，再把局部演示与完整文字方案放在同一次方案确认中；用户看过方案后说“开动”才进入首次状态变更。完成陈述后还有第三道按候选触发的回灌确认，项目级更新与 workflow 级更新分别决定，没有候选就 `no-op` 且不要求回复。已有可信输入可以减少澄清内容，但不能静默越过用户确认，出现缺口则回到产生缺口的位置。

## 不变量

1. **结果优先**：先定义用户状态将怎样改变，再决定做什么；任务完成不能替代结果完成。
2. **最小充分**：默认采用能守住质量的最短路径。新增步骤、文档、测试、智能体或交互只为消除独立风险、未知或证据缺口而出现。修改现有系统时，只改变结果契约允许的改动面；其余可观察能力默认为保护面并优先复用原实现，能以边界和复用证明保真的，不做全量人工前后对比。共享边界确需改动时，扩大改动面与相邻回归，不用“局部”掩盖真实影响。
3. **结果承载**：业务流中的每个动作至少独立承载结果、风险、可恢复性或证据价值；价值重叠的动作合并，没有消费价值的动作退役。
4. **证据分层**：执行者返回的是候选事实；只有经过契约匹配和新鲜验证的内容，才能成为已接受证据。没有新鲜证据，不声称完成。
5. **确认驱动**：可调查的事实由模型调查，契约内安全可逆细节由模型代选。所有状态变更任务都必须让用户看见需求澄清、需求确认和方案确认；“我需要”“帮我做”“实现一下”只表达请求，不构成执行授权，方案出现前说的“开动”也不确认尚未展示的方案。需求契约经用户确认后形成契约就绪；局部演示与完整文字方案经用户确认后形成方案就绪。用户此后明确说“开动”，一次确认持续有效，契约内工作连续兑现；新事实实质改变结果、风险、代价或授权时再回到用户决定。
6. **授权不扩张**：未经授权，不触发范围外或不可逆副作用。已有范围清楚的持续交付授权不重复询问。
7. **单一控制面**：需要持久恢复时，`work.md` 是协调者唯一可写的当前状态投影；子任务执行者只返回胶囊和回执，不并发改写控制面。
8. **因果回流**：验真、执行或交付暴露缺口时，返回最早能修正它的环节；不把返工伪装成新阶段。
9. **推进责任**：协调者始终维护一个明确的下一动作及责任者。仍有安全、已授权且可执行的下一动作时，模型继续推进，不能把计划完成、子任务完成、测试通过、候选产出或给出回复建议当成终点；可由模型继续解决的事项不转问用户。只有目标与验收已成立且无需用户行动，或确实出现必须由用户承担的取舍、授权或外部解锁时，才交回控制权。
10. **模型自由度**：协议只规定必须回答的问题、触发条件、停止条件和责任边界。具体研究方法、问题顺序、拆分粒度、并发方式与验证组合由模型按任务选择。

## 最小充分运行

先判断请求是否真的要改变状态。纯问答、解释、只读审查或一次性查询直接回答，不启动本工作流。

四个核心环节是必须成立的结果门，不是四次固定文件加载。根入口与已有信息足以判断内容时可以快速形成候选，但状态变更前的用户确认不能由模型自判通过。只在当前结果门存在缺口、风险或协作需求时读取对应参考：

1. 目标与验收尚未形成契约就绪，或按需边界会改变方案时，读取 [目标框定](references/frame.md)。
2. 推荐方案、结果覆盖、依赖或验真路径需要形成方案就绪时，读取 [结果规划](references/plan.md)。
3. 单一、局部任务可直接执行；实施边界需要展开时读取 [任务执行](references/execute.md)，只有出现多责任、独立复核或真实并行收益时才读取 [任务编排](references/orchestrate.md)。
4. 声明完成前必须满足结果验真门；证据覆盖或失效边界不显然时读取 [结果验真](references/prove.md)。
5. 只有存在真实外部交付目标时读取 [真实交付](references/deliver.md)。每个面向用户的目标完成陈述后进行一次最小优化识别；出现可行动候选或上下文压缩时读取 [经验复盘](references/learn.md)。上下文压缩作为治理信号，使本次复盘自然包含工作规则、项目入口、信息组织和业务动作的最小充分检查；压缩本身不预设存在缺陷或需要持久化结果。
6. 进入一个未登记当前兼容代际的现有项目，或项目登记显示需要升级时，读取 [项目初始化](references/initialize.md)。先做常数级状态检查；只有出现候选时才做有界清单与整体兼容改造，兼容代际未变化时不因 workflow 修复版本或普通任务重复诊断。

简单任务只有在对需求理解正确的把握超过 90%，目标与验收明确、没有关键承重决策、没有冲突真源、改动局部可逆，且不涉及生产、安全、破坏性操作或新增外部副作用时，才可把需求确认和方案确认合并为一次确认。合并时仍需展示最小演示或前后对比并等待用户回复；任一条件不成立就走两次标准确认。

只读取当前决定所需的参考，不预读整套文件。每份参考都是渐进路由器：先做最小动作并检查停止条件，再由真实触发信号展开深度。

每个环节产出候选或接受结果后，协调者立即判断下一就绪责任：下一动作属于模型且仍在契约与授权内，就直接继续推进；只在下一动作需要用户决策、范围外授权或无法自行取得的外部输入时暂停。计划、回执、测试绿灯和本地候选都是推进输入，不是独立终点。

## 结果责任与缺口回流

| 环节 | 必须负责的结果 | 通过依据 | 缺口回到 |
|---|---|---|---|
| 目标框定 | 目标、验收与关键取舍清楚，需求已经用户确认 | 承重未知已解决或逐项委托，并有有效 `requirement_receipt` | 目标框定内的研究、质询或体验探索 |
| 结果规划 | 演示与完整方案已确认，结果覆盖完整，任务具备依赖、责任和证据路径 | 每项验收都有实现责任与验真路径，并有有效 `solution_receipt` | 目标框定或计划自身 |
| 任务执行 | 产物真实产生，候选证据和偏差如实返回 | 子任务契约满足，失败没有被隐藏 | 结果规划、恢复或上游缺口 |
| 结果验真 | 子任务、整体结果与用户状态均有新鲜证据 | 已接受证据覆盖结果契约，而非只覆盖改动清单 | 最早产生缺口的环节 |
| 真实交付 | 目标环境中的交付状态可确认、可恢复 | 授权、项目发布门与交付后检查通过 | 执行、验真或安全停止 |
| 经验复盘 | 完成陈述后识别优化，只保留能改变未来行动的经验 | 有消费场景、事实证据、唯一责任者与入口；向用户展示问题诊断与推荐方案，实际回灌另走结果门，否则 `no-op` | 对应责任者、用户确认或无操作 |

交付成功不反向证明业务结果；验真成功也不自动授权交付。两者证据和授权分别成立。

## 渐进路由

| 当前需要解决的问题 | 读取 |
|---|---|
| 框定真实结果并判断还缺什么 | [目标框定](references/frame.md) |
| 缺少会改变决定的事实 | [最小研究](references/research.md) |
| 存在承重假设、矛盾或高代价取舍 | [深度质询](references/grill.md)；`grill-me` 是同一入口的兼容别名 |
| 体验、信息结构或交互可能改变方向 | [体验探索](references/experience.md) |
| 从结果形成计划、依赖与任务契约 | [结果规划](references/plan.md) |
| 决定串并联、责任分配与上下文边界 | [任务编排](references/orchestrate.md) |
| 实施当前就绪任务并返回回执 | [任务执行](references/execute.md) |
| 失败、证据冲突或连续尝试无进展 | [恢复处理](references/recover.md) |
| 声明任务、计划或业务结果完成 | [结果验真](references/prove.md) |
| 提交、合并、部署、发布或外部写入 | [真实交付](references/deliver.md) |
| 完成陈述后识别优化，形成待确认方案 | [经验复盘](references/learn.md) |
| 首次接入现有项目或兼容代际升级 | [项目初始化](references/initialize.md) |

目标框定中的研究、质询和体验探索不是默认并行的三道工序。三者都无触发信号就直接进入结果规划；只有事实充分性不足时才研究，承重决策不稳时才质询，体验会改变方向时才探索。多路同时触发时，通常先稳定共享事实；某一路的新结果只有实质改变了结论，才重开受影响的另一路。

## 用户汇报

汇报是业务状态的投影，不是内部执行日志。workflow 实际启动后，首次进展、环节实质变化、真实阻塞、最终交付和交回控制权这些关键出口展示“结论 + 四行画面”；状态未变不重复。同一轮即可完成的轻量任务可以只在最终出口展示一次，不为格式增加中途消息。

结论｜新版工作流已安全启用，后续任务会按新规则执行
进度｜■■■■｜■— · 核心结果完成 · 已完成真实更新 · 本次未发现追加优化
技能｜[任务执行](references/execute.md) · [结果验真](references/prove.md) · [真实交付](references/deliver.md)
成果｜✓ 旧版已安全替换 · ✓ 新版已确认可正常使用 · ✓ 没有产生重复安装
路径｜确认更新目标 → 安全替换旧版 → 验证实际可用

- `结论` 先说用户状态发生了什么，以及最重要的业务含义。
- `进度` 展示当前用户可见环节。前四格对应核心主链；真实进入交付或复盘时才在分隔线后追加。`■ / ◆ / □ / —` 分别表示完成、当前、未完成、不适用。
- `技能` 只列本轮实际使用的中文能力名；链接供需要时展开，不用工具名冒充结论。
- `成果` 只写用户能理解的已接受结果、实际影响和可用状态。每项都应能回答“对用户意味着什么”；不能只写文件数、内部对象、命令名或检查器名称。
- `路径` 用业务结果或用户动作描述最短活动路径。内部 `P/T` 标识只在确有定位价值时作为补充，不能代替人能读懂的名称。

主画面的每句话至少回答一个问题：发生了什么、对谁有什么影响、现在是否可用、有什么风险、是否需要行动。答不上来就删除或移到按需细节。

用户文字以让人真正看懂并帮助决定和行动为目的。优先使用用户熟悉的词和具体结果；专业术语确有定位价值时，先解释实际含义，再给出术语。理解程度由目标用户能否说清发生了什么、影响什么、怎样决定或行动来判断，不设置固定字数、术语数量或机械可读性指标。

技术证据用于建立可信度，不占据业务主视图。`runtime`、`Manifest`、`doctor`、文件数量、测试命令、哈希或内部标识只有会改变决定、风险或对结论的信心时才展开；先翻译业务含义，例如把“校验器通过”写成“已确认可以正常使用”，再在正文的按需证据中保留原始技术信息。

每个关键出口都标明下一步责任者。下一步属于模型时，说明即将采取的动作并继续推进，不把进展汇报变成暂停。下一步属于用户时，才交回控制权，并把所有相关选择合并到一次决策边界，给出一份紧凑决策包：“待决策 → 推荐下一步及理由 → 备选与取舍 → 不决定的影响 → 最短回复”。最短回复应允许用户直接接受推荐、选择备选或明确委托模型决定。没有风险、待决策或用户行动就不制造栏目。

完成出口先让用户明确知道原目标是否已经成立，再自动进行一次优化识别。有可行动发现时，继续给出问题诊断、实际影响、推荐方案和验收方式，把是否启动优化交给用户确认；没有发现时用一句话收口。内部测试、提交等微步骤不各自触发这一出口。

## 工作真源

简单和标准任务优先把计划、胶囊、回执留在当前上下文，不为形式建文档。只有跨会话、并行协作、昂贵证据或恢复成本真实存在时，才建立最小持久真源：

- `work.md`：协调者唯一可写的结果契约、当前计划、依赖、确认凭证、状态、已接受证据和阻塞。长任务记录绑定需求契约身份的 `requirement_receipt`、绑定演示/方案身份与交付路径的 `solution_receipt`，以及按项目级和 workflow 级分别选择的 `writeback_receipt`；普通任务把同等凭证留在对话。它是当前投影，不是过程日志。
- 任务胶囊：默认作为委派消息内联；只有需要跨上下文恢复时才物化，并由协调者登记定位。
- 执行回执：默认内联返回；昂贵或不可复现证据可物化。回执始终是候选，协调者验收后才进入 `work.md` 的已接受区。

不建立多个重复描述同一状态的文档。旧格式只在读取边界兼容；第一次写入 3.0 真源后，以 `work.md` 为准，不反向维护旧文件。

长项目完成的分支压缩为结果级证据入口，不保留过程流水；整体完成后退役当前 `work.md`，让长期真源承接最终结果。

## 角色、授权与降级

- **协调者**：持有整体结果，维护 `work.md`，接受或拒绝候选回执，决定下一就绪责任。
- **计划责任者**：保证结果覆盖、依赖和整体验真，不用子任务绿灯替代整体结果。
- **任务责任者**：只在胶囊授权和文件域内实施，如实返回改动、证据、失败和上游缺口。
- **复核者**：依据新鲜产物和结果契约独立判断，不继承执行者的完成结论。

角色可以由同一模型顺序承担，也可以交给多个子智能体；职责边界不随可用工具改变。没有子智能体就单体执行，没有持久存储就留在上下文，没有版本控制就交付文件与证据。能力降级不能把未覆盖写成通过。

## 兼容与维护

读取旧项目时可以识别旧阶段和旧计划文件，但不得继续扩散旧写法。项目初始化以兼容代际和现有责任者为真源，不按每个 workflow 版本重复扫描。修改本工作流时，先写能失败的协议测试，再同步所有写入者、读取者、验证器和发布清单；生成物只从正式真源重建。
